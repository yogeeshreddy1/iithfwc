int A=0,B=0;
int difference,borrow;


void setup() {
    pinMode(2, OUTPUT);  
    pinMode(3, OUTPUT);
   
}


void loop() {
  
 difference=(!A&&B) || (!B&&A);
borrow=(!A&&B);

  digitalWrite(2, difference); 
  digitalWrite(3, borrow); 
  }